defmodule Rumbl.Repo.Migrations.CascadeDeleteNullifyForBeardAndCategory do
  use Ecto.Migration

  def change do
    # alter table(:beards) do
    #   # todo: how to add cascade delete nilify to category after the fact?
    # end
  end
end
